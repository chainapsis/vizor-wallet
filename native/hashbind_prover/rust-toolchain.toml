# Must match the toolchain the solver's provekit pin builds with
# (shielded-zwap-e2z-proxy → provekit cc391c8 → rust-toolchain.toml).
# The FRB crate in rust/ stays on stable; only this crate needs nightly.
[toolchain]
channel = "nightly-2026-03-04"
